{
  "name": "siyuan-knowledge-ai",
  "version": "0.3.1",
  "private": true,
  "description": "Local-first SiYuan AI assistant with S3-synced index shards.",
  "scripts": {
    "check": "node --check index.js && node --check lib/core.js && node --check scripts/build.js && node --check scripts/generate-assets.js",
    "test": "node test/core.test.js",
    "build": "node scripts/generate-assets.js && node scripts/build.js",
    "pack": "npm run build"
  },
  "keywords": [
    "siyuan",
    "plugin",
    "ai",
    "rag",
    "openai"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "siyuan": "^1.2.1"
  }
}
